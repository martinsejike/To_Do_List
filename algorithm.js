function addAndCheckOffTasks() {
  var task = prompt("Enter a new task: ");
  // Code to append the task to the list of tasks and set the status to "incomplete"
  // Code to check off the task if it's checked off
}
